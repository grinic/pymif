pymif.napari._convert_widget
============================

.. py:module:: pymif.napari._convert_widget


Classes
-------

.. autoapisummary::

   pymif.napari._convert_widget.EmittingStream


Functions
---------

.. autoapisummary::

   pymif.napari._convert_widget.conservative_compute_multiscale_level
   pymif.napari._convert_widget.dataset_reader
   pymif.napari._convert_widget._dataset_axes
   pymif.napari._convert_widget._axis_index
   pymif.napari._convert_widget._axis_size
   pymif.napari._convert_widget._scale_for_axes
   pymif.napari._convert_widget._chunk_shape_for_dataset
   pymif.napari._convert_widget._present_range
   pymif.napari._convert_widget._set_range_widget
   pymif.napari._convert_widget.get_chunk_size
   pymif.napari._convert_widget.get_n_levels
   pymif.napari._convert_widget._run_conversion
   pymif.napari._convert_widget.convert_worker
   pymif.napari._convert_widget.convert_widget


Module Contents
---------------

.. py:function:: conservative_compute_multiscale_level(requested_shape, shape_threshold, downsample_factors)

   Computed desired level of the multiscale given requested field of view.

   The level of the multiscale should be the lowest resolution such that
   the requested shape is above the shape threshold. By passing a shape
   threshold corresponding to the shape of the canvas on the screen this
   ensures that we have at least one data pixel per screen pixel, but no
   more than we need.

   Parameters
   ----------
   requested_shape : tuple
       Requested shape of field of view in data coordinates
   shape_threshold : tuple
       Maximum size of a displayed tile in pixels.
   downsample_factors : list of tuple
       Downsampling factors for each level of the multiscale. Must be increasing
       for each level of the multiscale.

   Returns
   -------
   level : int
       Level of the multiscale to be viewing.


.. py:class:: EmittingStream

   Bases: :py:obj:`qtpy.QtCore.QObject`


   Qt-compatible text stream used to forward console output to the widget.


   .. py:attribute:: text_written


   .. py:method:: write(text)

      Emit written text to the Qt signal.



   .. py:method:: flush()

      Provide a file-like ``flush`` method for compatibility.



   .. py:method:: isatty()

      Report that this stream is not an interactive terminal.



.. py:function:: dataset_reader(microscope)

   Return the PyMIF manager class matching the selected microscope key.


.. py:function:: _dataset_axes(dataset)

   Return normalized dataset axes, defaulting to legacy TCZYX metadata.


.. py:function:: _axis_index(dataset, axis)

.. py:function:: _axis_size(dataset, axis, default=1)

.. py:function:: _scale_for_axes(dataset, requested_axes)

   Return a napari scale tuple for the requested spatial axis labels.


.. py:function:: _chunk_shape_for_dataset(dataset, chunk_z=16, chunk_y=512, chunk_x=512)

   Map widget Z/Y/X chunk values onto whatever axes the dataset has.


.. py:function:: _present_range(dataset, axis, requested_range)

   Return a clipped inclusive range tuple for a present axis, else None.


.. py:function:: _set_range_widget(widget, size, enabled=True)

.. py:function:: get_chunk_size(dataset_size, max_size_mb=100, axes='tczyx')

   Estimate a chunk shape for any dataset axis subset.

   The returned tuple follows ``axes``.  Legacy callers that pass a five-value
   TCZYX size still receive a five-value chunk tuple.


.. py:function:: get_n_levels(dataset_size, axes='tczyx')

   Estimate a default number of pyramid levels from present spatial axes.


.. py:function:: _run_conversion(reader, path, scene_index, chunks, t_range, z_range, y_range, x_range, channels, n_levels, downscale_factor, output_path, file_format, zarr_format)

   Run the conversion pipeline used by the napari worker thread.


.. py:function:: convert_worker(**kwargs)

   Background worker wrapper used by the napari conversion widget.


.. py:function:: convert_widget()

   Create the main PyMIF napari conversion widget.


